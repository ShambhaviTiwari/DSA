
def rotateMatrix(mat : List[List[int]]):
    #transpose
 
    for i in range(len(mat)):
        for j in range(i):
            mat[i][j],mat[j][i] = mat[j][i],mat[i][j]

    # reverse

    for i in range(len(mat)):
        mat[i].reverse()
    
